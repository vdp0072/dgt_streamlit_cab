# Copy this file to .streamlit/secrets.toml (DO NOT commit the real file)
# Replace the placeholder values with your Supabase project values

# Supabase project URL (found in Supabase dashboard)
SUPABASE_URL = "https://your-project-ref.supabase.co"

# Supabase ANON public key (safe for auth flows, but keep secret)
SUPABASE_ANON_KEY = "your-anon-key-here"

# Full DATABASE_URL (service role connection) - OPTIONAL for server-side ingestion
# Use the service role URL if you plan to run server-side ingestion from Streamlit.
# Keep this secret and only set it in Streamlit Cloud Secrets or local secrets.toml
DATABASE_URL = "postgresql://postgres:your_db_password@db.xxx.supabase.co:5432/postgres"
